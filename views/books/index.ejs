<!-- views/books/index.ejs -->

<section class="py-5">
  <div class="container px-4 px-lg-5 mt-5">
    <!-- 컨테이너 박스 -->
    <div class="row gx-4 gx-lg-5 row-cols-2 row-cols-md-3 row-cols-xl-4 justify-content-center"
      style="padding-bottom: 200px;">
      <% books.forEach(book=> { %>
        <div class="col mb-5">
          <div class="card h-100">
            <a href="/books/<%= book.id %>">
              <img class="card-img-top" src="<%= book.image %>" alt="<%= book.title %>" height="300px">
            </a>

            <div class="card-body p-4">
              <div class="text-center">
                <h5 class="fw-bolder">
                  <%= book.title %>
                </h5>
                <h6 class="fw-normal wandohopeR">작가: <span class="fw-bolder">
                    <%= book.author %>
                  </span></h6>
                <h6 class="fw-normal wandohopeR">장르: <span class="fw-bolder">
                    <%= book.genre %>
                  </span></h6>
                <h6 class="fw-normal wandohopeR">책의 한문장</h6>
                <p>
                  <span class="">
                    <%= book.one_words %>
                  </span>
                </p>


              </div>
            </div>
            <div class="card-footer p-4 pt-0 border-top-0 bg-transparent">
              <div class="text-center">
                <a class="btn btn-sm btn-outline-primary" role="button" href="/books/<%= book._id %>/edit">✏️ Edit</a>
                <a class="btn btn-sm btn-outline-danger" role="button"
                  href="/books/<%= book._id %>/delete?_method=DELETE"
                  onclick="return confirm('Are you sure you want to delete this record?')">🗑️ Delete</a>
              </div>
            </div>
          </div>
        </div>
        <% }); %>
    </div>
    <div class="text-center" style="margin-top: -200px;">
      <a href="/books/new" class="btn btn-primary">Add New Book</a>
      <a href="/" class="btn ut-red disabled" role="button" aria-disabled="true">Delete All Books</a>
    </div>
  </div>
</section>