<!-- views/games/edit -->



<main class="user-profile">
  <div class="container col-xl-10 col-xxl-8 px-4 py-5">
    <div class="pt-5">
      <h1>
        Game Data for <%= game.title %>
          <span class="btn btn-outline-danger">Editing</span>
      </h1>

      <form class="user-edit-form mb-3" method="POST" action="<%= `/games/${game._id}/update?_method=PUT` %>">
        <div class="row">
          <div class="col col-lg-2" style="position: relative">
            <figure class="user-img mt-5 mb-3">
              <img src="<%= game.image || " https://via.placeholder.com/150" %>" alt="Placeholder image"
              class="rounded mx-auto d-block"/>
            </figure>
            <span class="btn profileImg-edit-btn btn-outline-danger" id="profileImg-edit-button">✏️</span>
            <input type="text" name="image" id="inputImage" class="form-control profileImg-edit-input"
              value="<%= game.image %>" placeholder="URL for profile image" />
          </div>

          <!-- 게임 편집 폼 출력 -->
          <div class="col col-lg-10">
            <!-- Listing 20.4 (p. 292) -->
            <!-- 게임 데이터 update로의 폼 추가 -->
            <table class="table table-striped table-hover mt-5 mb-5">
              <!-- 사용자 데이터를 보여주기 위한 테이블 추가 -->
              <tbody>
                <tr>
                  <th scope="col">제목</th>
                  <td colspan="2" class="pt-0 pb-0 px-0">
                    <input type="text" name="title" id="inputTitle" class="form-control" value="<%= game.title %>"
                      placeholder="제목" autofocus />
                  </td>
                </tr>
                <tr>
                  <th scope="col">제작사</th>
                  <td colspan="2" class="pt-0 pb-0 px-0">
                    <input type="text" name="producer" id="inputProducer" class="form-control"
                      value="<%= game.producer %>" placeholder="제작사" autofocus />
                  </td>
                </tr>
                <tr>
                  <th scope="col">장르</th>
                  <td colspan="2" class="pt-0 pb-0 px-0">
                    <input type="text" name="genre" id="inputGenre" class="form-control" value="<%= game.genre %>"
                      placeholder="장르" autofocus />
                  </td>
                </tr>
                <tr>
                  <th scope="col">가격</th>
                  <td colspan="2" class="pt-0 pb-0 px-0">
                    <input type="text" name="cost" id="inputCost" class="form-control" value="<%= game.cost %>"
                      placeholder="가격" autofocus />
                  </td>
                </tr>
                <tr>
                  <th scope="col">Created</th>
                  <td colspan="2">
                    <%= game.createdAt %>
                  </td>
                </tr>
                <tr>
                  <th scope="col">Updated</th>
                  <td colspan="2">
                    <%= game.updatedAt %>
                  </td>
                </tr>
                <tr>
                  <th scope="col">ID</th>
                  <td colspan="2">
                    <%= game._id %>
                  </td>
                </tr>
              </tbody>
            </table>
            <button type="submit" class="btn btn-info">💾 Update Game</button>
            <a href="/games/<%= game._id %>/update?_method=PUT" class="btn btn-danger">❌ Cancel</a>

          </div>
        </div>
      </form>

      <!--
        Listing 20.10 (p. 299)
        CRUD 액션을 위한 링크 추가
      -->
      <div class="row">
        <div class="col col-lg-10 offset-lg-2">
          <!-- 프로필 페이지에서 사용자 계정의 편집 및 삭제를 위한 링크 추가 -->
          <a href="/games" class="btn btn-primary">← Back to Games</a>
          <a href="/games/<%= game._id %>/delete?_method=DELETE"
            onclick="return confirm('Are you sure you want to delete this record?')" class="btn btn-danger"
            role="button">🗑️ Delete Game</a>
        </div>
      </div>
    </div>
  </div>
</main>